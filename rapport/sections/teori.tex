\section{Teori}

I denne seksjonen presenteres det teoretiske grunnlaget for laboratorieøvelsen. Først gjennomgås sentrale prinsipper i grunnleggende kretsanalyse, samt virkemåten til vanlige dioder og zenerdioder. Deretter beskrives teori knyttet til klippekretser, enveis og toveis likeretting, DC-verdi og rippel, før de aktuelle kretsene som analyseres i rapporten presenteres.

\subsection{Formål og bakgrunn}

Formålet med denne laboratorieøvelsen er å undersøke hvordan dioder oppfører seg i ulike elektroniske kretser ved vekselspenning (AC). Dette inkluderer analyse av både vanlige dioder og zenerdioder, samt deres anvendelse i klippekretser og likeretterkretser.
\\[1em]
Et sentralt mål i øvelsen er å lese av nødvendige diodeparametere fra datablad, og bruke disse i analyse av kretsene. Videre er hensikten å oppnå forståelse for både kobling og virkemåte til klippekretser, enveis og toveis likeretterkretser, samt likeretterkretser med RC-ledd.
\\[1em]
Det legges også vekt på å sammenligne teoretiske beregninger med praktiske målinger ved bruk av oscilloskop og multimeter, og dermed vurdere avvik mellom ideelle modeller og virkelige komponenter.

\subsection{Datablad}
For å kunne bruke silisiumsdiode 1N4148 og zenordioder fra serien BZX55 må vi kjenne til deres nøkkelparametere. Disse parameterne inkluderer blant annet maksimal effekt, zenerspenning, og maksimal fremspenning. Disse verdiene er essensielle for design og analyse av kretser som involverer dioder. Vi kan bla opp i deres respektive datablader for å finne disse verdiene. Datablader i referanser~\cite{datasheetBZX55,datasheet1N4148}.

\begin{table}[H]
\centering
\caption{Nøkkelparametere for BZX55/C6V8.}
\label{tab:diode_params}
\begin{tabular}{lccc}
\toprule
\textbf{Parameter} & \textbf{Verdi} & \textbf{Betingelse} & \textbf{Enhet} \\
\midrule

Maks effekt $P_{max}$ & 500mW &  & W \\

Zenerspenning $V_Z$ & 6.8V & $I_Z = 5\text{mA}$ & V \\

Maks fremspenning $V_F$ & 1.0V & $I_F = 100\text{mA}$ & V \\

\bottomrule
\end{tabular}
\end{table}

\begin{table}[H]
\centering
\caption{Nøkkelparametere for 1N4148.}
\label{tab:diode_params_1N4148}
\begin{tabular}{lccc}
\toprule
\textbf{Parameter} & \textbf{Verdi} & \textbf{Betingelse} & \textbf{Enhet} \\
\midrule

Maks effekt $P_{max}$ & 500mW &  & W \\

Maks fremspenning $V_F$ & 1.0V & $I_F = 100\text{mA}$ & V \\

Reversstrøm $I_R$ & 25nA & $V_R = 20\text{V}$ & A \\

\bottomrule
\end{tabular}
\end{table}




\subsection{Teoretisk grunnlag i grunnleggende kretsanalyse}
Før vi kan analysere dioder og deres anvendelser, er det viktig å forstå grunnleggende prinsipper for kretsanalyse, inkludert Ohms lov og Kirchhoffs lover. Disse prinsippene danner grunnlaget for å kunne beregne strøm, spenning og effekt i elektriske kretser, og er essensielle for å kunne forstå hvordan dioder påvirker kretsens oppførsel.

\subsubsection{Ohms lov}
Ohms lov beskriver sammenhengen mellom spenning (V), strøm (I) og motstand (R) i en elektrisk krets. Loven sier at strømmen gjennom en metallisk leder med konstant temperatur er proporsjonal med den elektriske potensialforskjellen (spenningen) over lederen, og omvendt proporsjonal med motstanden. Matematisk uttrykkes dette som:
\begin{equation}
    V = I \cdot R
\end{equation}
\noindent
hvor $I$ er strømmen gjennom lederen målt i ampere (A), $V$ er spenningen over lederen målt i volt (V), og $R$ er motstanden til lederen målt i ohm ($\Omega$). Kretsen under viser en enkel krets der alle størrelsene inngår.

\begin{figure}[H]
\centering
\begin{circuitikz}[american voltages]
    \node[circle, draw=black, fill=none, thick, inner sep=2pt] (A) at (-1,2) {};
    \node[circle, fill=black, inner sep=0pt] (B) at (1,2) {};
    \node[circle, fill=black, inner sep=0pt] (C) at (1,-2) {};
    \node[circle, draw=black, fill=none, thick, inner sep=2pt] (D) at (-1,-2) {};

    \node at (-1, 1.6) {$+$};
    \node at (-1, -1.6) {$-$};
    \node at (-1, 0) {$V$};

    \draw (A) to[short, -, i^=$I$] (B);
    \draw (B) to[R=$R$] (C);
    \draw (C) to[short, -] (D);

\end{circuitikz}
\caption{Illustrasjon av Ohms lov i en enkel krets.}
\label{fig:ohm}
\end{figure}

\paragraph{Anvendelse i kretsanalyse. } Ohms lov utgjør grunnlaget for analyse i elektriske kretser. Loven benyttes sammen med andre lover for å bestemme ukjente strømmer, spenninger og motstander i komplekse kretser. I kretser med dioder vil Ohms lov være verktøyet for å kunne beregne strøm og spenninger for å få et teoretisk grunnlag for å kunne sammenligne med praktiske målinger.

\subsubsection{Kirchhoffs lover}
Kirchhoffs lover består av to grunnleggende prinsipper for elektriske kretser: \textbf{Kirchhoffs strømlov} (KCL) og \textbf{Kirchhoffs spenningslov} (KVL). Disse lovene beskriver konservering av elektrisk ladning og energi, og er essensielle for å kunne analysere og forstå komplekse kretser, inkludert de som inneholder dioder. 

\paragraph{Kirchhoffs strømlov (KCL). } Denne loven beskriver konservasjon av elektrisk ladning. KCL sier at summen av strømmer som kommer inn i et nodepunkt i en elektrisk krets, er lik summen av strømmer som går ut av det samme nodepunktet.

\begin{figure}[H]
\centering
\begin{circuitikz}[american voltages]
    \node[circle, fill=black, inner sep=1.2pt] (A) at (0, 0) {};
    \node[circle, fill=black, inner sep=0pt] (B) at (0, 3) {};
    \node[circle, fill=black, inner sep=0pt] (C) at (3, 0) {};
    \node[circle, fill=black, inner sep=0pt] (D) at (0, -3) {};
    \node[circle, fill=black, inner sep=0pt] (E) at (-3, 0) {};


    \draw (A) to[R=$R_1$, i^=$i_1$] (B);
    \draw (C) to[R=$R_2$, i^=$i_2$] (A);
    \draw (D) to[R=$R_3$, i^=$i_3$] (A);
    \draw (A) to[R=$R_4$, i^=$i_4$] (E);
    

\end{circuitikz}
\caption{Illustrasjon av et forgreningspunkt med inn- og utgående strømmer.}
\label{fig:KCL}
\end{figure}

\[
\sum_{n=1}^{N} I_n = 0
\]
\\[1em]
\noindent
I figur~\ref{fig:KCL} vil KCL kunne uttrykkes som $i_2 + i_3 - i_1 - i_4 = 0$.

\paragraph{Kirchhoffs spenningslov (KVL). } Denne loven beskriver konservasjon av elektrisk energi. KVL sier at summen av alle elektriske potensialendringer (spenninger) rundt en lukket sløyfe i en elektrisk krets, er lik null. I en sløyfe med $N$ forgreninger med spenninger $V_n$, kan KVL uttrykkes som:

\[
\sum_{n=1}^{N} V_n = 0
\]
\\[1em]
\noindent
Kretsen i figur~\ref{fig:KVL} viser en sløyfe med fire elementer. Ved å følge retningen av den elektriske strømmen, kan KVL uttrykkes som $-V_4 + V_1 + V_2 + V_3 = 0$.

\begin{figure}[H]
\centering
\begin{circuitikz}[american voltages]
    \node[circle, fill=black, inner sep=0pt] (A) at (-3, 2.5) {};
    \node[circle, fill=black, inner sep=0pt] (B) at (3, 2.5) {};
    \node[circle, fill=black, inner sep=0pt] (C) at (3, -2.5) {};
    \node[circle, fill=black, inner sep=0pt] (D) at (-3, -2.5) {};


    \draw (A) to[R=$R_1$, v_=$V_1$] (B);
    \draw (B) to[R=$R_2$, v_=$V_2$] (C);
    \draw (C) to[R=$R_3$, v_=$V_3$] (D);

    \draw (A) to[V=$V_4$] (D);

    \draw[->] (0, -1) arc[start angle=-90,end angle=-360,radius=1];

    \node at (0, 0) {$I$};

\end{circuitikz}
\caption{Illustrasjon av en sløyfe.}
\label{fig:KVL}
\end{figure}

\subsection{Dioder}
En diode er en to-terminal elektrisk komponent som leder elektrisk strøm primært i en retning. Dioden har lav motstand (ideelt null) i en retning og høy motstand (ideelt uendelig) i den motsatte retningen. Formålet med dioden i applikasjoner og kretser er å kontrollere retningen av elektrisk strøm~\cite{wikipediaDiodes}.

\begin{figure}[H]
\centering
\begin{circuitikz}[american voltages]
    \node[circle, fill=black, inner sep=0pt] (A) at (-2, 0) {};
    \node[circle, fill=black, inner sep=0pt] (B) at (2, 0) {};

    \draw (A) to[D, v_=$V_D$, i^=$I_D$] (B);

  \end{circuitikz}
\caption{Symbolet for diode i kretser.}\label{fig:Diode}
\end{figure}

\paragraph{Diodens oppbygning. } For å oppnå formålet med en diode, er den konstruert med en p-n-overgang. Dette betyr at dioden består av to halvledere, en type \textbf{p (positiv)} og en type \textbf{n (negativ)}. Dette er oppnådd ved å \textbf{dope} et halvledermateriale. Doping er prosessen hvor meget rene halvledere tilsettes kontrollerte mengder av urenheter. Dette påvirker krystallstrukturen til stoffet. I en p-type halvleder er det et overskudd av hull (positive ladningsbærere), mens i en n-type halvleder er det et overskudd av elektroner (negative ladningsbærere)~\cite{wikipediaDoping}. Katoden er den negative terminalen i en diode, og anoden er den positive terminalen. I praksis betyr dette at overskuddet av ladningsbærerne fra n-siden vil forflytte seg til ledige hull på p-siden, som gir opphav til elektroner i bevegelse i primært én retning.
\\[1em]
Ser vi på en silisiumsdiode med silisiumsstruktur, vil doping av silisium med fosfor (P) skape en n-type halvleder, hvor det er et overskudd av elektroner. Disse ekstra elektronene er frie til å bevege seg gjennom krystallstrukturen. Doper vi silisium med bor (B), vil det skape en p-type halvleder, hvor det er et overskudd av hull. Disse hullene fungerer som positive ladningsbærere~\cite{learningBook}.

\begin{figure}[H]
\centering

\begin{subfigure}{0.45\textwidth}
\centering
\begin{tikzpicture}
% --- N-TYPE ---
 % bonds
  \draw[gray] (0,0) -- (2,0) -- (4,0);
  \draw[gray] (0,2) -- (2,2) -- (4,2);
  \draw[gray] (0,4) -- (2,4) -- (4,4);
  \draw[gray] (0,0) -- (0,2) -- (0,4);
  \draw[gray] (2,0) -- (2,2) -- (2,4);
  \draw[gray] (4,0) -- (4,2) -- (4,4);
  \draw[gray] (2,2) -- (2.7,2.9);

  % silicon lattice
  \foreach \x in {0,2,4} {
    \foreach \y in {0,2,4} {
      \node[draw, circle, fill=gray!20, minimum size=8mm] at (\x,\y) {\small Si};
    }
  }

  % dopant atom
  \node[draw, circle, fill=blue!20, minimum size=8mm] at (2,2) {\small P};

  % extra electron
  \node[draw, circle, fill=blue!10, inner sep=1.5pt, label=right:{\small fritt elektron}] at (2.7,2.9) {$e^-$};
\end{tikzpicture}
\caption{n-type doping}
\end{subfigure}
\hfill
\begin{subfigure}{0.45\textwidth}
\centering
\begin{tikzpicture}
% --- P-TYPE ---
% bonds
  \draw[gray] (0,0) -- (2,0) -- (4,0);
  \draw[gray] (0,2) -- (2,2);
  \draw[gray] (0,4) -- (2,4) -- (4,4);
  \draw[gray] (0,0) -- (0,2) -- (0,4);
  \draw[gray] (2,0) -- (2,2) -- (2,4);
  \draw[gray] (4,0) -- (4,2) -- (4,4);
  \draw[gray] (4,2) -- (3, 2);

    % silicon lattice
\foreach \x in {0,2,4} {
    \foreach \y in {0,2,4} {
      \node[draw, circle, fill=gray!20, minimum size=8mm] at (\x,\y) {\small Si};
    }
  }

  % dopant atom
  \node[draw, circle, fill=red!20, minimum size=8mm] at (2,2) {\small B};

  % hole
  \node[draw, circle, dashed, fill=white, inner sep=2pt, label=above:{\small hull}] at (2.80,2) {$h^+$};
\end{tikzpicture}
\caption{p-type doping}
\end{subfigure}

\caption{Illustrasjon av doping i halvledere}
\end{figure}

\noindent
Disse materialene er satt sammen i en p-n-overgang for å danne en diode. Elektronene og hullene vil diffundere over grenseflaten mellom de to materialene. Dette skaper et område kalt \textbf{deplasjonsregionen}, hvor det er et elektrisk felt som hindrer videre diffusjon av ladningsbærere. Når en spenning påføres i fremoverretning (positiv spenning på anoden), vil dette elektriske feltet reduseres, og dioden vil etter nok spenningsforskjell over dioden, begynne å lede strøm. Når en spenning påføres i revers retning (positiv spenning på katoden), vil det elektriske feltet øke, og dioden vil ikke lede strøm, bortsett fra en liten lekkasjestrøm. Denne lekkasjestrømmen er vanligvis ubetydelig og blir ikke tatt høyde for i teoretiske beregninger~\cite{learningBook}.

\paragraph{Diodens karakteristiske kurve. } Diodens karakteristiske kurve viser forholdet mellom strømmen gjennom dioden og spenningen over den. I fremoverretning vil strømmen øke eksponentielt etter at en terskelspenning (for silisiumdiode, ca. 0.7 V) er nådd. I revers retning vil strømmen være svært liten, og vi får i praksis en lekkasjestrøm~\cite{wikipediaDiodes}.

\begin{figure}[H]
\centering

\begin{tikzpicture}
\begin{axis}[
    width=15cm,
    height=12cm,
    xmin=-9, xmax=15,
    ymin=-5, ymax=16,
    axis lines=middle,
    axis line style={black, very thick},
    xlabel={$V_D\;[\mathrm{V}]$},
    ylabel={$I_D\;[\mathrm{mA}]$},
    xtick={-9,-8,...,15},
    xticklabels={
    {}, $-40$, {}, $-30$, {}, $-20$, {}, $-10$, {}, $0$,
    {}, {}, $0.3$, {}, $0.5$, {}, $0.7$, {}, {}, $1.0$, {}, {}, {}, {}
    },
    ytick={-5,-4,...,16},
    yticklabels={$-10\unit{pA}$, $-8\unit{pA}$, $-6\unit{pA}$, $-4\unit{pA}$, $-2\unit{pA}$, $0$, $1$, $2$, $3$, {}, $5$, {}, {}, {}, {}, $10$, {}, {}, {}, {}, {}, {},},
    yticklabel style={xshift=4pt, anchor=west},
    grid=both,
    major grid style={gray!50, line width=0.4pt},
    minor grid style={gray!30, line width=0.2pt},
    tick style={draw=none},
    minor tick num=0,
    enlargelimits=false,
    clip=false
]

\addplot[
    blue,
    very thick,
    smooth
] coordinates {
    (0,0)
    (3,0.1)
    (5,0.9)
    (7,4)
    (8,16)
};

\addplot[
    blue,
    very thick,
    smooth
] coordinates {
    (-1,-5)
    (-0.2, -4.8)
    (0,-4)
};

\addplot[
    blue,
    very thick
] coordinates {
    (-9,-5)
    (-1,-5)
};

\addplot[
    blue,
    very thick
] coordinates {
    (0,-4)
    (0,0)
};

\draw[black!70, thick] (axis cs:7,-5) -- (axis cs:7,16);

\end{axis}
\end{tikzpicture}

\caption{Silisiumdiodes karakteristiske kurve~\cite{learningBook}.}\label{fig:silisiumsDiodeKurve}
\end{figure}

\noindent
Diodens karakteristiske kurve viser sammenhengen mellom spenninngen over dioden ($V_D$) og strømmen gjennom dioden ($I_D$). Kurven gir oss dermed en rekke \textbf{arbeidspunkter} langs kurven for dioden. Om en gitt spenning er påført dioden, kan vi finne den tilsvarende strømmen ved å se på kurven. Kurven for silisiumsdioder i figur~\ref{fig:silisiumsDiodeKurve} viser at i fremoverretning, etter at terskelspenningen på ca. 0.7 V er nådd, øker strømmen kraftig. I revers retning er strømmen svært liten (lekkasjestrøm). For teoretiske beregninger i denne rapporten vil 0.7 V brukes som terskelspenning for silisiumsdioder.

\paragraph{Polarisering. } I dioder har vi tre forskjellige tilstander: fremoverpolarisering (forward-bias), reverspolarisering (reverse-bias) og upolarisering (no-bias). I den karakteristiske kurven for dioden vil fremoverpolarisering være for $V_D > 0\text{V}$, reverspolarisering for $V_D < 0\text{V}$ og upolarisering for $V_D = 0\text{V}$. I kretsanalyse vil tilfellene være som i figurene under.

\begin{figure}[H]
\centering

\begin{subfigure}{0.29\textwidth}
\centering
\begin{circuitikz}[american voltages]
    \node[circle, fill=black, inner sep=1.5pt, label=above:{$5\mathrm{V}$}] (A) at (-2,0) {};
    \node[circle, fill=black, inner sep=1.5pt, label=above:{$0\mathrm{V}$}] (B) at (2,0) {};

    \draw (A) to[D, i>^=$I_D$, v_=$V_D$] (B);
\end{circuitikz}
\caption{Fremoverpolarisering.}
\end{subfigure}
\hfill
\begin{subfigure}{0.29\textwidth}
\centering
\begin{circuitikz}[american voltages]
    \node[circle, fill=black, inner sep=1.5pt, label=above:{$0\mathrm{V}$}] (A) at (-2,0) {};
    \node[circle, fill=black, inner sep=1.5pt, label=above:{$5\mathrm{V}$}] (B) at (2,0) {};

    \draw (A) to[D, v_=$V_D$] (B);
\end{circuitikz}
\caption{Reverspolarisering.}
\end{subfigure}
\hfill
\begin{subfigure}{0.29\textwidth}
\centering
\begin{circuitikz}[american voltages]
    \node[circle, fill=black, inner sep=1.5pt, label=above:{$0\mathrm{V}$}] (A) at (-2,0) {};
    \node[circle, fill=black, inner sep=1.5pt, label=above:{$0\mathrm{V}$}] (B) at (2,0) {};

    \draw (A) to[D, v_=$V_D$] (B);
\end{circuitikz}
\caption{Upolarisert tilstand.}
\end{subfigure}

\caption{Diode ved ulike nodepotensialer.}
\label{fig:diode_bias_nodes}
\end{figure}

\subsubsection{Zenerdioder}

En zenordiode er en spesiell type diode som er designet for å lede strøm i revers retning når en bestemt spenning, kalt zenerspenningen, er nådd. Siden zenerdioder er konstruert for å operere i reverspolarisering, har de en karakteristisk kurve som viser at når zenerspenningen er nådd, vil strømmen øke kraftig i revers retning. Zenerdioder brukes ofte i spenningsregulatorer og beskyttelseskretser for å sikre at spenningen ikke overstiger et bestemt nivå~\cite{wikipediaZenerDiodes}.

\begin{figure}[H]
\centering
\begin{circuitikz}[american voltages]
    \node[circle, fill=black, inner sep=0pt] (A) at (-2, 0) {};
    \node[circle, fill=black, inner sep=0pt] (B) at (2, 0) {};

    \draw (A) to[zzD, v_=$V_D$, i^=$I_D$] (B);

  \end{circuitikz}
\caption{Symbolet for zenerdiode i kretser.}\label{fig:ZenerDiode}
\end{figure}

\noindent
I figur~\ref{fig:ZenerDiode} vil zenordioden teoretisk lede strøm dersom $V_D > 0.7\text{V}$. Zenordioden vil også lede strøm i revers retning dersom $V_D < -V_Z$, hvor $V_Z$ er zenerspenningen.
\paragraph{Zenordiodens karakteristiske kurve. } Denne er lik silisiumsdioden i fremoverpolarisering, men i reverspolariseringsområdet vil den kunne operere når zenorspenningen er nådd.

\begin{figure}[H]
\centering

\begin{tikzpicture}
\begin{axis}[
    width=15cm,
    height=10cm,
    xmin=-11, xmax=11,
    ymin=-8, ymax=8,
    axis lines=middle,
    axis line style={black, very thick},
    xlabel={$V_D\;[\mathrm{V}]$},
    ylabel={$I_D\;[\mathrm{mA}]$},
    xtick={-11,-10,...,11},
    xticklabels={
    {}, {}, {}, {}, {}, {}, {}, {}, {}, {}, {}, $0$,
    {}, {}, $0.3$, {}, $0.5$, {}, $0.7$, {}, {}, $1.0$, {}, {}, {}, {}
    },
    ytick={-8,-7,...,8},
    yticklabels={
        {},{},{},$-5$,{},{},$-2$,$-1$,$0$,$1$,$2$,{},{},$5$,{},{},{}
    },
    yticklabel style={xshift=4pt, anchor=west},
    grid=both,
    major grid style={gray!50, line width=0.4pt},
    minor grid style={gray!30, line width=0.2pt},
    tick style={draw=none},
    minor tick num=0,
    enlargelimits=false,
    clip=false
]

\addplot[
    blue,
    very thick,
    smooth
] coordinates {
    (0,0)
    (3,0.1)
    (5,0.9)
    (7,4)
    (7.5,8)
};

\addplot[
    blue,
    very thick,
    smooth
] coordinates {
    (-0.5,-0.5)
    (-0.2, -0.4)
    (0,0)
};

\addplot[
    blue,
    very thick
] coordinates {
    (-4.8,-0.5)
    (-0.5,-0.5)
};

\addplot[
    blue,
    very thick,
    smooth
] coordinates {
    (-4.8, -0.5)
    (-5.2, -0.6)
    (-5.5, -0.8)
    (-5.8, -1.2)
    (-6.0, -2.0)
    (-6.1, -3.5)
    (-6.2, -5.5)
    (-6.3, -6.8)
    (-6.35, -8.0)
};

\node at (-7, 0.7) {$-V_Z$};

\draw[black!70, thick] (axis cs:7,-8) -- (axis cs:7,8);

\draw[black!70, thick] (axis cs:-6,-8) -- (axis cs:-6,8);

\end{axis}
\end{tikzpicture}

\caption{Zenerdiodes karakteristiske kurve~\cite{learningBook}.}\label{fig:zenerdiodeKurve}
\end{figure}

\noindent
Dioden lekker noe strøm ved alle negative spenninger, men i teoretiske beregninger i denne rapporten vil dette ikke tas høyde for. I tillegg kan vi se at kurven blir nærmest vertikal ved $V_D = -V_Z$, noe som indikerer at dioden vil lede en stor strøm for små endringer i spenning rundt zenerspenningen, og at spenningen er nærmest konstant. Dette gjør zenerdioden spesielt nyttig for spenningsregulering, da den kan opprettholde en stabil spenning over seg selv til tross for variasjoner i strømmen som går gjennom den~\cite{wikipediaZenerDiodes}.

\subsection{Klippekretser}\label{teo:klippekrets}
Klippekretser er kretser som bruker dioder for å ``klippe'' eller begrense spenningen i en krets til et bestemt nivå. Dette kan være nyttig for å beskytte komponenter mot overspenning eller for å forme signaler. Klippekretser kan være enveis eller toveis, avhengig av om de begrenser spenningen i én halvbølge eller begge halvbølgene til AC-signalet~\cite{wikipediaClippers}.
\\[1em]
Klippekretsen konstrueres ved å plassere en diode i serie med en motstand, og deretter koble denne kombinasjonen i parallell med lasten. Bruker vi en vanlig diode, vil klippekretser begrense spenningen i fremover retning på 0.7V. Bruker vi en zenerdiode, kan vi begrense spenningen i både fremover på 0.7V og revers retning på zenorspenningen~\cite{wikipediaClippers}.

\begin{figure}[H]
\centering
\begin{circuitikz}
\draw
(0,0) to[sV, l={$V_{in}(t)$}] (0,3)
      to[R, l=$R$] (5,3)
      to[D, a=$D$] (5,0)
      to[short] (0,0);

\node[circle, draw=black, fill=none, thick, inner sep=2pt] (Vtop) at (7,3) {};
\node[circle, draw=black, fill=none, thick, inner sep=2pt] (Vbot) at (7,0) {};

\draw (5,3) to[short] (Vtop) node[below] {$+$};
\draw (5,0) to[short] (Vbot) node[above] {$-$};

\node at (7, 1.5) {$V_{out}(t)$};


\node at (0, 0)[ground] {};
\end{circuitikz}
\caption{Enveis klippekrets.}
\label{fig:clipper_circuit}
\end{figure}

\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
    width=11cm,
    axis lines=middle,
    xlabel={$t$},
    ylabel={$V$},
    xmin=0, xmax=6.5,
    ymin=-1.2, ymax=1.2,
    samples=200,
    domain=0:6.28,
    ytick={-1,-0.5,0,0.7,1},
    yticklabels={$-1$, $-0.5$, $0$, $0.7$, $1$},
    xtick={3.14,6.28},
    xticklabels={$T/2$, $T$},
    legend style={
        at={(0.98,0.98)},
        anchor=north east,
        draw=none,
        fill=none
    }
]
\addplot[
    blue,
    thick
] {sin(deg(x))};
\addlegendentry{$V_{in}$}

\addplot[
    red,
    thick
] {min(sin(deg(x)), 0.7)};
\addlegendentry{$V_{out}$}

\end{axis}
\end{tikzpicture}
\caption{Inngangs- og utgangsspenning for klippekretsen i figur~\ref{fig:clipper_circuit}. Inngangssignalet $V_{in}$ er sinusformet, mens utgangssignalet $V_{out}$ begrenses til $0.7\mathrm{V}$ når dioden leder.}
\label{fig:clipper_in_out}
\end{figure}

\subsection{Likeretting}
Likeretting er prosessen med å omdanne vekselspenning (AC) til likespenning (DC). Dioder brukes ofte i likeretterkretser for å oppnå dette. Det finnes flere typer likeretterkretser, inkludert enveis og toveis likeretting, som hver har sine egne karakteristikker og bruksområder~\cite{wikipediaRectifiers}.

\subsubsection{Énveis likeretter}\label{teo:enveis_likeretter}
I énveis likeretting vil enten den positive eller negative halvbølgen av AC-signalet bli tillatt å passere gjennom, mens den andre halvbølgen blokkeres. Dette er oppnådd ved å plassere en diode i serie med lasten. Når den positive halvbølgen av AC-signalet påføres, vil dioden lede og tillate strøm å passere gjennom lasten. Når den negative halvbølgen påføres, vil dioden blokkere strømmen, og ingen strøm vil passere gjennom lasten~\cite{wikipediaRectifiers}. Kretsen og utgangssignalet for en énveis likeretter er vist i figur~\ref{fig:one_way_rectifier} og figur~\ref{fig:one_way_rectifier_in_out}.

\vspace{1em}

\begin{figure}[H]
\centering
\begin{circuitikz}
\draw
(0,0) to[sV, l={$V_{in}(t)$}] (0,3)
      to[D] (5,3)
      to[R, l=$R_L$] (5,0)
      to[short] (0,0);

\node[circle, draw=black, fill=none, thick, inner sep=2pt] (Vtop) at (7,3) {};
\node[circle, draw=black, fill=none, thick, inner sep=2pt] (Vbot) at (7,0) {};

\draw (5,3) to[short] (Vtop) node[below] {$+$};
\draw (5,0) to[short] (Vbot) node[above] {$-$};

\node at (7, 1.5) {$V_{out}(t)$};


\node at (0, 0)[ground] {};
\end{circuitikz}
\caption{Énveis likeretterkrets.}
\label{fig:one_way_rectifier}
\end{figure}

\vspace{1em}

\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
    width=11cm,
    axis lines=middle,
    xlabel={$t$},
    ylabel={$V$},
    xmin=0, xmax=6.5,
    ymin=-6, ymax=6,
    samples=200,
    domain=0:6.28,
    ytick={-5,-2.5,0,2.5,4.3,5},
    yticklabels={$-5$, $-2.5$, $0$, $2.5$, $4.3$, $5$},
    xtick={3.14,6.28},
    xticklabels={$T/2$, $T$},
    legend style={
        at={(0.98,0.98)},
        anchor=north east,
        draw=none,
        fill=none
    }
]
\addplot[
    blue,
    thick
] {5 * sin(deg(x))};
\addlegendentry{$V_{in}$}

\addplot[
    red,
    thick
] {max(5 * sin(deg(x)) - 0.7, 0)};
\addlegendentry{$V_{out}$}

\addplot[dashed, gray] coordinates {(0,4.3) (1.57,4.3)};

\end{axis}
\end{tikzpicture}
\caption{Inngangs- og utgangsspenning for énveis likeretterkretsen i figur~\ref{fig:one_way_rectifier}.}
\label{fig:one_way_rectifier_in_out}
\end{figure}


\subsubsection{Toveis likeretter}\label{teo:toveis_likeretter}
I toveis likeretting vil både den positive og negative halvbølgen av AC-signalet passere gjennom med samme fortegn. Dette oppnås ved å bruke en bro av fire dioder, kjent som en bro-likeretter. Broen sikrer at strøm gjennom lastmotstanden vil alltid være i samme retning, uavhengig av polariteten til inngangssignalet~\cite{wikipediaRectifiers}. Kretsen og utgangssignalet for en toveis likeretter er vist i figur~\ref{fig:two_way_rectifier} og figur~\ref{fig:two_way_rectifier_in_out}. For at strøm skal ledes må to dioder skrus på, dette skjer da inngangsspenningen er større enn 1.4V. Det betyr at utgangssignalet vil være. 
\[
V_{out}(t) = \max \left(0, \, V_{in}(t) - 1.4\text{V}\right)
\]

\begin{figure}[H]
\centering
\begin{circuitikz}

    \draw (0,0) to[D] (2,2);
    \draw (0,0) to[D] (2,-2);
    \draw (2,2) to[D] (4,0);
    \draw (2,-2) to[D] (4,0);

    \draw (4, 0) to[short] (5, 0) to[R, l=$R_L$] (5,-3) to[short] (0,-3) to[short] (0,0);
    \draw (2,-2) to[sV, l={$V_{in}(t)$}] (2,2);.

    \node[circle, draw=black, fill=none, thick, inner sep=2pt] (Vtop) at (7,0) {};
    \node[circle, draw=black, fill=none, thick, inner sep=2pt] (Vbot) at (7,-3) {};

    \draw (5,0) to[short] (Vtop) node[below] {$+$};
    \draw (5,-3) to[short] (Vbot) node[above] {$-$};

    \node at (7, -1.5) {$V_{out}(t)$};

\end{circuitikz}
\caption{Toveis likeretterkrets.}
\label{fig:two_way_rectifier}
\end{figure}

\noindent
Under er en fremstilling av strømmens vei gjennom kretsen for både den positive og negative halvbølgen av inngangssignalet $V_{in}(t)$:

\vspace{3em}

\begin{figure}[H]
\centering

\begin{subfigure}[t]{0.48\textwidth}
\centering
\begin{circuitikz}[scale=0.9]

    % Bridge
    \draw (0,0) to[D] (2,2);
    \draw (0,0) to[D] (2,-2);
    \draw (2,2) to[D] (4,0);
    \draw (2,-2) to[D] (4,0);

    % Load + source
    \draw (4,0) to[short] (5,0) to[R, l=$R_L$] (5,-3) to[short] (0,-3) to[short] (0,0);
    \draw (2,-2) to[sV, l={$V_{in}(t)$}] (2,2);

    % Voltage markers
    \node[circle, draw=black, fill=none, thick, inner sep=2pt] (Vtop) at (6.3,0) {};
    \node[circle, draw=black, fill=none, thick, inner sep=2pt] (Vbot) at (6.3,-3) {};
    \draw (5,0) to[short] (Vtop) node[below] {$+$};
    \draw (5,-3) to[short] (Vbot) node[above] {$-$};
    \node at (6.9,-1.5) {$V_{out}(t)$};

    % Current direction arrows: positive half-wave
    \draw[->, very thick, red] (2,1.2) -- (2,1.8);      % up through source
    \draw[->, very thick, red] (2.35,1.65) -- (3.65,0.35); % through upper-right diode
    \draw[->, very thick, red] (4.4,0) -- (4.9,0);      % to resistor
    \draw[->, very thick, red] (5,-0.7) -- (5,-2.3);    % down through RL
    \draw[->, very thick, red] (4.5,-3) -- (0.5,-3);    % return path bottom wire
    \draw[->, very thick, red] (0.35,-0.35) -- (1.65,-1.65); % through lower-left diode

\end{circuitikz}
\caption{Positiv halvbølge.}
\end{subfigure}
\hfill
\begin{subfigure}[t]{0.48\textwidth}
\centering
\begin{circuitikz}[scale=0.9]

    % Bridge
    \draw (0,0) to[D] (2,2);
    \draw (0,0) to[D] (2,-2);
    \draw (2,2) to[D] (4,0);
    \draw (2,-2) to[D] (4,0);

    % Load + source
    \draw (4,0) to[short] (5,0) to[R, l=$R_L$] (5,-3) to[short] (0,-3) to[short] (0,0);
    \draw (2,-2) to[sV, l={$V_{in}(t)$}] (2,2);

    % Voltage markers
    \node[circle, draw=black, fill=none, thick, inner sep=2pt] (Vtop) at (6.3,0) {};
    \node[circle, draw=black, fill=none, thick, inner sep=2pt] (Vbot) at (6.3,-3) {};
    \draw (5,0) to[short] (Vtop) node[below] {$+$};
    \draw (5,-3) to[short] (Vbot) node[above] {$-$};
    \node at (6.9,-1.5) {$V_{out}(t)$};

    % Current direction arrows: negative half-wave
    \draw[->, very thick, red] (2,-1.2) -- (2,-1.8);        % down through source
    \draw[->, very thick, red] (2.35,-1.65) -- (3.65,-0.35); % through lower-right diode
    \draw[->, very thick, red] (4.4,0) -- (4.9,0);          % to resistor
    \draw[->, very thick, red] (5,-0.7) -- (5,-2.3);        % down through RL
    \draw[->, very thick, red] (4.5,-3) -- (0.5,-3);        % return path bottom wire
    \draw[->, very thick, red] (0.35,0.35) -- (1.65,1.65);  % through upper-left diode

\end{circuitikz}
\caption{Negativ halvbølge.}
\end{subfigure}

\caption{Strømmens vei gjennom brokoblingen for de to halvperiodene av inngangssignalet.}
\label{fig:bridge_current_paths}
\end{figure}






\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
    width=11cm,
    axis lines=middle,
    xlabel={$t$},
    ylabel={$V$},
    xmin=0, xmax=6.5,
    ymin=-6, ymax=6,
    samples=200,
    domain=0:6.28,
    ytick={-5,-2.5,0,2.5,3.6,5},
    yticklabels={$-5$, $-2.5$, $0$, $2.5$, $3.6$, $5$},
    xtick={3.14,6.28},
    xticklabels={$T/2$, $T$},
    legend style={
        at={(0.98,0.98)},
        anchor=north east,
        draw=none,
        fill=none
    }
]
\addplot[
    blue,
    thick
] {5 * sin(deg(x))};
\addlegendentry{$V_{in}$}

\addplot[
    red,
    thick
] {max(abs(5*sin(deg(x))) - 1.4, 0)};
\addlegendentry{$V_{out}$}

\addplot[dashed, gray] coordinates {(0,3.6) (4.71,3.6)};

\end{axis}
\end{tikzpicture}
\caption{Inngangs- og utgangsspenning for toveis likeretterkretsen i figur~\ref{fig:two_way_rectifier}.}
\label{fig:two_way_rectifier_in_out}
\end{figure}


\subsubsection{DC-verdi og rippel}\label{teo:dc_og_rippel}

Likespenning (DC) er en spenning med konstant polaritet, i motsetning til vekselspenning (AC) som varierer periodisk med tiden. Dersom det er en AC-spenning med et periodisk signal, er DC-verdien av denne spenningen definert som den gjennomsnittlige verdien av spenningen over en periode. For å finne DC-verdien av en AC-spenning, kan vi derfor integrere over en periode for å finne arealet under kurven, og deretter dele dette på perioden for å få gjennomsnittet~\cite{learningBook}. Matematisk utledet formel for DC-verdien av en AC-spenning $V(t)$ over en periode $T$ er som formel~\ref{eq:DCfromAC}.
\begin{equation}\label{eq:DCfromAC}
    V_{DC} = \frac{1}{T} \int_0^T V(t)  dt
\end{equation}

\noindent
Rippel (rippelspenning) er den gjenværende periodiske variasjonen i en likespenning som er utledet fra en vekselspenningskilde. Denne variasjonen oppstår fordi den vekselspente komponenten ikke blir fullstendig fjernet etter likeretterprosessen. Rippelspenningen stammer dermed fra den pulserende utgangen fra likeretteren, og kan reduseres ved bruk av filtrering, for eksempel ved hjelp av en kondensator parallelt med lasten~\cite{wikipediaRipple}. Rippelspenningen er derfor et mål på forskjell i toppunkt og bunnpunkt i utgangsspenningen etter likeretterprosessen, og kan uttrykkes som formel~\ref{eq:ripple}.
\begin{equation}\label{eq:ripple}
    V_{ripple} = V_{peak} - V_{min}
\end{equation}


\subsection{Glatting med kondensator~--~RC-ledd}\label{teo:glatting_kondensator}
Kondensatoren kan brukes i en likeretterkrets for å glatte ut utgangsspenningen og redusere rippel. Når en kondensator kobles i parallell med lasten i en likeretterkrets, vil den lade opp når utgangsspenningen stiger, og deretter avgi lagret energi når utgangsspenningen faller. Dette bidrar til å opprettholde en mer konstant spenning over lasten, og reduserer dermed rippel~\cite{wikipediaRectifiers}.

\begin{figure}[H]
\centering
\begin{circuitikz}

\draw (0,0) to[D] (2,2);
\draw (0,0) to[D] (2,-2);
\draw (2,2) to[D] (4,0);
\draw (2,-2) to[D] (4,0);

\draw (4, 0) to[short] (5, 0) to[R, l=$R_L$] (5,-3) to[short] (0,-3) to[short] (0,0);
\draw (2,-2) to[sV, l={$V_{in}(t)$}] (2,2);.

\node[circle, draw=black, fill=none, thick, inner sep=2pt] (Vtop) at (9,0) {};
\node[circle, draw=black, fill=none, thick, inner sep=2pt] (Vbot) at (9,-3) {};

\draw (5,0) to[short] (Vtop) node[below] {$+$};
\draw (5,-3) to[short] (Vbot) node[above] {$-$};

\draw (7,0) to[C, l=$C$] (7,-3);

\node at (9, -1.5) {$V_{out}(t)$};

\end{circuitikz}
\caption{Toveis likeretterkrets med bruk av kondensator for å redusere rippel.}
\label{fig:rectifier_with_capacitor}
\end{figure}

\noindent
Kretsen for en toveis likeretter med kondensator for glatting er vist i figur~\ref{fig:rectifier_with_capacitor}. I denne kretsen vil kondensatoren $C$ lade opp når utgangsspenningen stiger, og deretter avgi lagret energi når utgangsspenningen faller, noe som resulterer i en mer konstant spenning over lasten $R_L$. Utgangsspenningen uten kondensator vil være lik den teoretiske utgangspenningen for en toveis likeretter, som vist i figur~\ref{fig:two_way_rectifier_in_out}. Med kondensatoren vil utgangsspenningen være mer konstant og nærmere den maksimale verdien av inngangsspenningen. Dette resulterer i en høyere og mer stabil DC-verdi, samt en betydelig reduksjon i rippel. Inngangs- og utgangsspenningen for toveis likeretterkretsen med kondensator er vist i figur~\ref{fig:rectifier_with_capacitor_in_out}.



\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
    width=15cm,
    height=9cm,
    axis lines=middle,
    xlabel={$t$},
    ylabel={$V$},
    xmin=0, xmax=16.5,
    ymin=-1, ymax=5,
    samples=500,
    domain=0:15.7,
    ytick={0,3.6},
    yticklabels={$0$, $V_M$},
    xtick={3.14,6.28, 9.42, 12.56, 15.7},
    xticklabels={$T/2$, $T$, $3T/2$, $2T$, $5T/2$},
    legend style={
        at={(0.98,0.98)},
        anchor=north east,
        draw=none,
        fill=none
    }
]

% Uten kondensator
\addplot[
    blue,
    thick
] {max(0, abs(5 * sin(deg(x))) - 1.4)};
\addlegendentry{$V_{out} \: \text{(uten kondensator)}$}

% Med kondensator: første opplading
\addplot[
    red,
    thick,
    domain=0.284:pi/2+0.1
] {abs(5 * sin(deg(x))) - 1.4};
\addlegendentry{$V_{out} \: \text{(med kondensator)}$}

% Periode 1
\addplot[
    red,
    thick
] coordinates {
    (1.507, 3.6)
    (4.312, 3.206)
};

\addplot[
    red,
    thick,
    domain=3/2*pi-0.4:3/2*pi
] {abs(5 * sin(deg(x))) - 1.4};

% Periode 2
\addplot[red, thick] coordinates {
    (4.649, 3.6)
    (7.454, 3.206)
};
\addplot[red, thick, domain={5/2*pi-0.4}:{5/2*pi}] {abs(5*sin(deg(x))) - 1.4};

% Periode 3
\addplot[red, thick] coordinates {
    (7.791, 3.6)
    (10.596, 3.206)
};
\addplot[red, thick, domain={7/2*pi-0.4}:{7/2*pi}] {abs(5*sin(deg(x))) - 1.4};

% Periode 4
\addplot[red, thick] coordinates {
    (10.933, 3.6)
    (13.738, 3.206)
};
\addplot[red, thick, domain={9/2*pi-0.4}:{9/2*pi}] {abs(5*sin(deg(x))) - 1.4};

\end{axis}
\end{tikzpicture}
\caption{Inngangs- og utgangsspenning for toveis likeretterkretsen i figur~\ref{fig:rectifier_with_capacitor}.}
\label{fig:rectifier_with_capacitor_in_out}
\end{figure}

\noindent
For en toveis likeretter med kondensator for glatting, vil peak-til-peak rippelspenningen kunne uttrykkes som:
\begin{equation}\label{eq:capacitor_ripple}
\Delta V_{ripple} = \frac{I_{L}}{f C} = \frac{V_{DC}}{R_L f C}
\end{equation}

\noindent
Der $\Delta V_{ripple}$ er rippelspenningen, $I_L$ er strømmen gjennom lasten, $f$ er frekvensen av inngangssignalet, $C$ er kapasitansen til kondensatoren, og $V_{DC}$ er den gjennomsnittlige utgangsspenningen over lasten. Formelen viser at rippelspenningen kan reduseres ved å øke kapasitansen $C$, redusere belastningsstrømmen $I_L$, eller øke frekvensen $f$ av inngangssignalet~\cite{wikipediaRipple}.


\subsection{Kretser}
Vi vil nå se på seks forskjellige kretser som involverer dioder, og analysere deres oppførsel og utgangssignaler. Disse kretsene inkluderer både klippekretser og likeretterkretser, og vil gi oss innsikt i hvordan dioder kan brukes til å forme og kontrollere elektriske signaler. Vi vil senere sammenlikne de praktiske målingene med de teoretiske utregningene for hver krets, og diskutere eventuelle avvik og årsakene til disse. Signalgeneratoren bruker frekvensen 50Hz, og teoretiske grafer og beregninger vil bruke dette.

\subsubsection{Krets 1 -- enveis klippekrets}

\begin{figure}[H]
\centering

\begin{circuitikz}
    \draw
    (0,0) to[sV, l={$V_{in}(t)$}, a=$20V_{pp}$] (0,3)
          to[R, l=$R_1$, a=$180\Omega$] (5,3)
          to[D, l=$D_3$, a=$\text{1N4148}$] (5,0)
          to[short] (0,0);

    \node[circle, draw=black, fill=none, thick, inner sep=2pt] (Vtop) at (7,3) {};
    \node[circle, draw=black, fill=none, thick, inner sep=2pt] (Vbot) at (7,0) {};

    \draw (5,3) to[short] (Vtop) node[below] {$+$};
    \draw (5,0) to[short] (Vbot) node[above] {$-$};

    \node at (7, 1.5) {$V_{out}(t)$};

    \node at (0, 0)[ground] {};
\end{circuitikz}

\caption{Enkel klippekrets}\label{fig:krets1}
\end{figure}

\vspace{1em}

\noindent
Kretsen i figur~\ref{fig:krets1} er en enkel klippekrets som bruker en enkelt diode (D3) for å begrense utgangsspenningen. Kretsen er kjent fra klippekretsteorien i seksjon~\ref{teo:klippekrets}. Siden kretsen ikke leder strøm før $D_3$ skrur på, vil utgangssignalet $V_{out}(t)$ være lik inngangssignalet $V_{in}(t)$ så lenge $V_{in}(t) < 0.7\text{V}$. Når $V_{in}(t)$ når 0.7 V, vil dioden begynne å lede, og utgangssignalet vil bli begrenset til 0.7V.

\vspace{2em}

\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
    width=11cm,
    axis lines=middle,
    xlabel={$t\:[ms]$},
    ylabel={$V\:[V]$},
    xmin=0, xmax=6.5,
    ymin=-12, ymax=12,
    samples=200,
    domain=0:6.28,
    ytick={-10,-5,0.7,5,10},
    yticklabels={$-10$, $-5$, $0.7$, $5$, $10$},
    xtick={3.14,6.28},
    xticklabels={$10$, $20$},
    legend style={
        at={(0.98,0.98)},
        anchor=north east,
        draw=none,
        fill=none
    }
]
\addplot[
    blue,
    thick
] {10 * sin(deg(x))};
\addlegendentry{$V_{in}$}

\addplot[
    red,
    thick
] {min(10 * sin(deg(x)), 0.7)};
\addlegendentry{$V_{out}$}

\end{axis}
\end{tikzpicture}
\caption{Inngangs- og utgangsspenning for klippekretsen i figur~\ref{fig:krets1}.}
\label{fig:krets1_in_out}
\end{figure}

\subsubsection{Krets 2 -- toveis klippekrets}
\begin{figure}[H]
\centering
\begin{circuitikz}
    \draw
    (0,0) to[sV, l={$V_{in}(t)$}, a=$20V_{pp}$] (0,3)
          to[R, l=$R_1$, a=$180\Omega$] (5,3)
          to[D, l=$D_3$, a=$\text{1N4148}$] (5,0)
          to[short] (0,0);

    \draw
    (5,0) to[short] (9,0)
          to[D, a=$D_5$, l=$\text{1N4148}$] (9,3)
          to[short] (5,3);

    \node[circle, draw=black, fill=none, thick, inner sep=2pt] (Vtop) at (11,3) {};
    \node[circle, draw=black, fill=none, thick, inner sep=2pt] (Vbot) at (11,0) {};

    \draw (9,3) to[short] (Vtop) node[below] {$+$};
    \draw (9,0) to[short] (Vbot) node[above] {$-$};

    \node at (11, 1.5) {$V_{out}(t)$};

    \node at (0, 0)[ground] {};

\end{circuitikz}
\caption{Toveis klippekrets}\label{fig:krets2}
\end{figure}

\vspace{1em}

\noindent
I denne kretsen vil utledet teori for klippekrets i seksjon~\ref{teo:klippekrets} tilsi at utgangssignalet $V_{out}(t)$ vil være lik inngangssignalet $V_{in}(t)$ så lenge $-0.7\text{V} < V_{in}(t) < 0.7\text{V}$. Dette er fordi her klipper en diode i hver spenningsretning. Når én diode skrur på, vil den andre forbli åpen krets for å konservere spenningen over parallellkoblingen av diodene. Utgangssignalet vil derfor være begrenset til 0.7V i positiv retning og -0.7V i negativ retning.

\vspace{3em}

\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
    width=11cm,
    axis lines=middle,
    xlabel={$t\:[ms]$},
    ylabel={$V\:[V]$},
    xmin=0, xmax=6.5,
    ymin=-12, ymax=12,
    samples=200,
    domain=0:6.28,
    ytick={-10,-5,-0.7, 0.7,5,10},
    yticklabels={$-10$, $-5$, $-0.7$, $0.7$, $5$, $10$},
    xtick={3.14,6.28},
    xticklabels={$10$, $20$},
    legend style={
        at={(0.98,0.98)},
        anchor=north east,
        draw=none,
        fill=none
    }
]
\addplot[
    blue,
    thick
] {10 * sin(deg(x))};
\addlegendentry{$V_{in}$}

\addplot[
    red,
    thick,
    domain=0:3.14
] {min(10 * sin(deg(x)), 0.7)};
\addlegendentry{$V_{out}$}

\addplot[
    red,
    thick,
    domain=3.14:6.28
] {max(10 * sin(deg(x)), -0.7)};

\end{axis}
\end{tikzpicture}
\caption{Inngangs- og utgangsspenning for klippekretsen i figur~\ref{fig:krets2}.}
\label{fig:krets2_in_out}
\end{figure}

\subsubsection{Krets 3 -- zenordioder i klippekrets}

\begin{figure}[H]
\centering
\begin{circuitikz}
    \draw 
    (0,0) to[sV, l={$V_{in}(t)$}, a=$20V_{pp}$] (0,6)
        to[R, l=$R_1$, a=$180\Omega$] (8,6);
    \draw (0, 0) to[short] (8,0);
    \draw (8,3) to[zzD, a=$D_4$, l=$\text{BZX55/C4V7}$] (8,6);
    \draw (8,3) to[zzD, l=$D_6$, a=$\text{BZX55/C6V8}$] (8,0);

    \node[circle, draw=black, fill=none, thick, inner sep=2pt] (Vtop) at (11,6) {};
    \node[circle, draw=black, fill=none, thick, inner sep=2pt] (Vbot) at (11,0) {};

    \draw (8,6) to[short] (Vtop) node[below] {$+$};
    \draw (8,0) to[short] (Vbot) node[above] {$-$};

    \node at (11, 3) {$V_{out}(t)$};

    \node at (0, 0)[ground] {};


\end{circuitikz}
\caption{Klippekrets med zenerdioder}\label{fig:krets3}
\end{figure}

\noindent
Kretsen i figur~\ref{fig:krets3} er en klippekrets som bruker zenerdioder for å begrense utgangsspenningen. Dersom vi først ser på den positive halvbølgen til inngangssignalet, vil $D_4$ først aktiveres når $V_{in}(t)$ blir 4.7V, og $D_6$ aktiveres når spenningspotensialet mellom zenordiodene er 0.7V høyere enn jording. Derfor må inngangsspenningen nå 5.4V før $D_6$ skrur på. Når $D_6$ skrur på, vil den ''klippe'' utgangssignalet til 5.4V. Når inngansspenningen er negativ vil akkurat samme logikk gjelde, men $D_6$ har zenorspenning på 6.8V, så inngangsspenningen må nå $-6.8V - 0.7V = -7.5V$ før $D_4$ skrur på og klipper utgangssignalet til -7.5V.

\vspace{2em}

\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
    width=11cm,
    axis lines=middle,
    xlabel={$t\:[ms]$},
    ylabel={$V\:[V]$},
    xmin=0, xmax=6.5,
    ymin=-12, ymax=12,
    samples=200,
    domain=0:6.28,
    ytick={-10,-7.5, 0,5.4,10},
    yticklabels={$-10$, $-7.5$, $0$, $5.4$, $10$},
    xtick={3.14,6.28},
    xticklabels={$10$, $20$},
    legend style={
        at={(0.98,0.98)},
        anchor=north east,
        draw=none,
        fill=none
    }
]
\addplot[
    blue,
    thick
] {10 * sin(deg(x))};
\addlegendentry{$V_{in}$}

\addplot[
    red,
    thick,
    domain=0:3.14
] {min(10 * sin(deg(x)), 5.4)};
\addlegendentry{$V_{out}$}

\addplot[
    red,
    thick,
    domain=3.14:6.28
] {max(10 * sin(deg(x)), -7.5)};

\addplot[dashed, gray] coordinates {(0,5.4) (0.6,5.4)};

\addplot[dashed, gray] coordinates {(0,-7.5) (4,-7.5)};



\end{axis}
\end{tikzpicture}
\caption{Inngangs- og utgangsspenning for klippekretsen i figur~\ref{fig:krets3}.}\label{fig:krets3_in_out}
\end{figure}


\subsubsection{Krets 4 -- enveis likeretter}
\begin{figure}[H]
\centering
\begin{circuitikz}

\draw
(0,0) to[sV, l={$V_{in}(t)$}, a=$20V_{pp}$] (0,3)
      to[D, l=$D_1$, a=$1\text{1N4148}$] (5,3)
      to[R, l=$R_L$, a=$1\text{k}\Omega$] (5,0)
      to[short] (0,0);


\node[circle, draw=black, fill=none, thick, inner sep=2pt] (Vtop) at (7,3) {};
\node[circle, draw=black, fill=none, thick, inner sep=2pt] (Vbot) at (7,0) {};

\draw (5,3) to[short] (Vtop) node[below] {$+$};
\draw (5,0) to[short] (Vbot) node[above] {$-$};

\node at (7, 1.5) {$V_{out}(t)$};

\node at (0, 0)[ground] {};

\end{circuitikz}
\caption{Enveis likeretterkrets}\label{fig:krets4}
\end{figure}

Dette er en enkel enveis likeretterkrets kjent fra seksjon~\ref{teo:enveis_likeretter}. Kretsen blokker strøm i negativ retning og tillater strøm i positiv retning. Utgangssignalet $V_{out}(t)$ vil derfor være 0V dersom inngangsignalet er negativt, og vil stige med inngangskurven dersom inngangssignalet er større enn 0.7V. Teoretiske utgangssignalet er identisk til figur~\ref{fig:one_way_rectifier_in_out} i seksjon~\ref{teo:enveis_likeretter}.

\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
    width=11cm,
    axis lines=middle,
    xlabel={$t$},
    ylabel={$V$},
    xmin=0, xmax=6.5,
    ymin=-12, ymax=12,
    samples=200,
    domain=0:6.28,
    ytick={-10,-5,0,5,9.3},
    yticklabels={$-10$, $-5$, $0$, $5$, $9.3$},
    xtick={3.14,6.28},
    xticklabels={$10$, $20$},
    legend style={
        at={(0.98,0.98)},
        anchor=north east,
        draw=none,
        fill=none
    }
]
\addplot[
    blue,
    thick
] {10 * sin(deg(x))};
\addlegendentry{$V_{in}$}

\addplot[
    red,
    thick
] {max(10 * sin(deg(x)) - 0.7, 0)};
\addlegendentry{$V_{out}$}

\addplot[dashed, gray] coordinates {(0,9.3) (1.57,9.3)};

\end{axis}
\end{tikzpicture}
\caption{Inngangs- og utgangsspenning for énveis likeretterkretsen i figur~\ref{fig:krets4}.}\label{fig:krets4_in_out}
\end{figure}

\paragraph{DC-spenning over lastmotstanden. } Vi bruker formel~\ref{eq:DCfromAC} fra seksjon~\ref{teo:dc_og_rippel}. Vi integrerer over nullpunktene til funksjonen $10\cdot \sin (50\cdot 2 \cdot \pi \cdot t) - 0.7$. Disse er $t = 0.000223\text{s}$ og $t = 0.00978\text{s}$. Perioden er som kjent $T = 0.02$s. Vi regner for DC-verdien.
\[
V_{DC} = \frac{1}{0.02}\int_{0.000223}^{0.00978} 10\sin (100\pi t) - 0.7  dt \rightarrow V_{DC} = 2.84\text{V}
\]

\noindent
Dersom vi flipper dioden for å oppnå en negativ énveis likeretterkrets, vil DC spenningen skifte fortegn. Dette er fordi grafen er da speilet om x-aksen.





\subsubsection{Krets 5 -- toveis likeretter}

\begin{figure}[H]
\centering
\begin{circuitikz}

    \draw (0,0) to[D] (2,2);
    \draw (0,0) to[D] (2,-2);
    \draw (2,2) to[D] (4,0);
    \draw (2,-2) to[D] (4,0);

    \draw (4, 0) to[short] (5, 0) to[R, l=$R_L$, a=$1\text{k}\Omega$] (5,-3) to[short] (0,-3) to[short] (0,0);
    \draw (2,-2) to[sV, l={$V_{in}(t)$}, a=$20V_{pp}$] (2,2);

    \node[circle, draw=black, fill=none, thick, inner sep=2pt] (Vtop) at (7,0) {};
    \node[circle, draw=black, fill=none, thick, inner sep=2pt] (Vbot) at (7,-3) {};

    \draw (5,0) to[short] (Vtop) node[below] {$+$};
    \draw (5,-3) to[short] (Vbot) node[above] {$-$};

    \node at (7, -1.5) {$V_{out}(t)$};


\end{circuitikz}
\caption{Toveis likeretterkrets}\label{fig:krets5}
\end{figure}

\noindent
Kretsen er lik kretsen i seksjon~\ref{teo:toveis_likeretter}. Denne kretsen gjør at strøm alltid går samme vei gjennom motstanden, og vi vil dermed få en teoretisk kurve lik den i figur~\ref{fig:two_way_rectifier_in_out} i seksjon~\ref{teo:toveis_likeretter}.

\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
    width=11cm,
    axis lines=middle,
    xlabel={$t\:[ms]$},
    ylabel={$V\:[V]$},
    xmin=0, xmax=6.5,
    ymin=-12, ymax=12,
    samples=200,
    domain=0:6.28,
    ytick={-10,-5,0,5,8.6, 10},
    yticklabels={$-10$, $-5$, $0$, $5$, $8.6$, $10$},
    xtick={3.14,6.28},
    xticklabels={$10$, $20$},
    legend style={
        at={(0.98,0.98)},
        anchor=north east,
        draw=none,
        fill=none
    }
]
\addplot[
    blue,
    thick
] {10 * sin(deg(x))};
\addlegendentry{$V_{in}$}

\addplot[
    red,
    thick
] {max(abs(10*sin(deg(x))) - 1.4, 0)};
\addlegendentry{$V_{out}$}

\addplot[dashed, gray] coordinates {(0,8.6) (4.71,8.6)};

\end{axis}
\end{tikzpicture}
\caption{Inngangs- og utgangsspenning for toveis likeretterkretsen i figur~\ref{fig:krets5}.}
\label{fig:krets5_in_out}
\end{figure}


\paragraph{DC-spenning over lastmotstanden. } Vi bruker formel~\ref{eq:DCfromAC} fra seksjon~\ref{teo:dc_og_rippel}. Siden utgangssignalet kan uttrykkes som et periodisk signal med halvparten av perioden til inngangssignalet, kan vi integrere i intervallet $0 < t < 10\text{ms}$ for å finne DC-verdien. Nullpunktene til funksjonen $10\cdot \sin (50\cdot 2 \cdot \pi \cdot t) - 1.4$ er $t = 0.000447\text{s}$ og $t = 0.00955\text{s}$. Vi regner for DC-verdien.
\[
V_{DC} = \frac{1}{0.01} \int_{0.000447}^{0.00955} (10\sin (100\pi t) - 1.4)  dt \rightarrow V_{DC} = 5.04\text{V}
\]


\subsubsection{Krets 6 -- toveis likeretter med kondensator}

\begin{figure}[H]
\centering
\begin{circuitikz}

\draw (0,0) to[D] (2,2);
\draw (0,0) to[D] (2,-2);
\draw (2,2) to[D] (4,0);
\draw (2,-2) to[D] (4,0);

\draw (4, 0) to[short] (5, 0) to[R, l=$R_L$, a=$1\text{k}\Omega$] (5,-3) to[short] (0,-3) to[short] (0,0);
\draw (2,-2) to[sV, l={$V_{in}(t)$}, a=$20V_{pp}$] (2,2);

\node[circle, draw=black, fill=none, thick, inner sep=2pt] (Vtop) at (9,0) {};
\node[circle, draw=black, fill=none, thick, inner sep=2pt] (Vbot) at (9,-3) {};

\draw (5,0) to[short] (Vtop) node[below] {$+$};
\draw (5,-3) to[short] (Vbot) node[above] {$-$};

\draw (7,0) to[C, l=$C_2$] (7,-3);

\node at (9, -1.5) {$V_{out}(t)$};

\end{circuitikz}
\caption{Toveis likeretterkrets med kondensator for glatting.}
\label{fig:krets6}
\end{figure}


\noindent
Kretsen i figur~\ref{fig:krets6} er en toveis likeretterkrets som bruker en kondensator for å glatte ut utgangsspenningen og redusere rippel. Utgangssignalet vil være mer konstant og nærmere den maksimale verdien av inngangsspenningen, noe som resulterer i en høyere og mer stabil DC-verdi, samt en betydelig reduksjon i rippel. Ser vi på formel~\ref{eq:capacitor_ripple} fra seksjon~\ref{teo:dc_og_rippel}:
\[
\Delta V_{ripple} = \frac{I_L}{f C} = \frac{V_{DC}}{R_L f C}
\]
\noindent kan vi se at rippelspenningen $\Delta V_{ripple}$ vil reduseres ved å øke kapasitansen $C$ til kondensatoren. Dette skyldes at en større kapasitans vil kunne lagre mer energi og dermed bedre opprettholde en konstant spenning over lasten.
\\[1em]
I forsøket vil vi teste kondensator med kapasitanser $10\mu\text{F}$ og $100\mu\text{F}$. Det vi forventer er at rippleverdien er mindre ved $100\mu\text{F}$ på grunn av kondensatorens evne til å lagre mer energi. Da vil vi også forvente en høyere DC-verdi for kretsen med $100\mu\text{F}$.